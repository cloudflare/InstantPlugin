.step(data-ref="steps[]" data-step="embedCode")
  p
    | This is the embed code your users add to their website to install your tool or service.
    | Don’t worry if it requires some user customization, you’ll be able to set that up in the next step.

  .input-container(data-flow="column")
    .demos
      span Or try a demo:

      button.demo.button.secondary(
        data-ref="navigationButtons[]"
        data-step="demo"
        data-demo="emojiReact"
        tabindex="0")  Emoji React 🎉

      button.demo.button.secondary(
        data-ref="navigationButtons[]"
        data-step="demo"
        data-demo="forecastIO"
        tabindex="0")  Forecast.io ☀️

    textarea.embed-code-input.box(
      autocapitalize="off"
      autocomplete="off"
      autocorrect="off"
      autofocus
      data-ref="embedCodeInput"
      placeholder="<script>, <iframe>, etc..."
      spellcheck="false")

  .actions
    button.button.secondary.transparent(data-ref="navigationButtons[]" data-step="intro") Back
    button.button.primary.next(data-ref="navigationButtons[]" data-step="attributePicker") Next
