section(data-component="image-uploader")
  .standard(filepicker)
    input(type="file" name="file" accept="image/*" id="image-uploader")
    label(class="button secondary transparent slim" for="image-uploader") Choose image...
